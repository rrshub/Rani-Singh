import { SelectCheckDirective } from './select-check.directive';

describe('SelectCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectCheckDirective();
    expect(directive).toBeTruthy();
  });
});
